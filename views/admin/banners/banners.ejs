<!DOCTYPE html>
<html lang="en">
<%- include('../../partials/head.ejs') %>

  <body class="admin-body">
    <%- include('../../partials/admin/admin-navbar.ejs') %>

  
    <%- include('../../partials/admin/admin-sidebar.ejs') %>

          <section class="container pt-5">
            <div class="row ">
             
              <div class="col-12 table-responsive ">
                <table id="datatable" class="table display table-overflow  table-borderless table-hover">
                  <thead>
                    <tr>
                      <th class="" scope="col">#</th>
                      <th class="" scope="col">Banner Title</th>
                      <th class="" scope="col">Banner Image </th>
                      <th class="" scope="col">Action</th>
    
                    </tr>
                  </thead>
                  <tbody>
    
                    <% if(banners.length>0) { %>
                      <% var count=1 %>
                        <% banners.forEach(items=> { %>
                          <tr>
                            <th scope="row">
                              <%= count++ %>
                            </th>
                            <td>
                              <%= items.bannerTitle %>
                            </td>
                            <td >
                             <img class="productImage" src="<%= items.images %>" >
                            </td>
                            <td>
                              <div>     
                                <button class="btn btn-outline-danger" id="deletebanner<%= count %> "
                                  onclick="deleteBanner(this.id)" data-url="<%= items._id %>">Delete</button>
                              </div>
                            </td>
                          </tr>
                          <% }) %>
                            <% } %>
                  </tbody>
                </table>
              </div>
            </div>
            <% if(banners.length<3) { %>
           
              <div class="mt-3">
                <form action="/admin/banners" method="post" enctype="multipart/form-data">
                  <h5 class="form-label  text-info-emphasis text-center" for="form2Example17">Add New Banner</h5>
                  <span class="text-danger">
                    <%= message %>
                  </span>
                  <div class="mb-2">
                    <input class="form-control w-50 form-control-lg text-secondary" name="banner" type="text"
                      placeholder="Banner Title" />
                    </div>
                      <div class="d-flex col-12">
                        <input type="file"  class="form-control form-control-sm mb-1"  name="banner" id="banner" required>
                     
                    <button class="btn btn-outline-dark btn-lg" type="submit"> Add </button>
                   </div>
                </form>
              </div>
              <% }else{ %>
                <p class="text-center fw-bold">Delete existing Banners to add new banners</p>
             <% } %>
          </section>
          <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
          <script src="https://cdn.datatables.net/1.13.1/js/jquery.dataTables.min.js"></script>
          <script src="/scripts/adminScript.js"></script>
          <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
            crossorigin="anonymous"></script>
      </body>

</html>